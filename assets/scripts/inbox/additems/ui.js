'use strict';

// const store = require('../../store');

const onAddItemSuccess = (data) => {
  // console.log(data);
};

const failure = (/*error*/) => {
};

module.exports = {
  failure,
  onAddItemSuccess,
};
